   <% if user_signed_in? %>
   <div class="col-sm-3">
     <div class="panel panel-default">
      <div class="title"><%= fa_icon 'bar-chart-o' %> Point Balance</div>
      <div class="panel-body">
        0
      </div>
    </div>
   </div>
   <div class="col-sm-7">
     <div class="panel panel-default">
      <div class="title"><%= fa_icon 'clock-o' %> Recent Activity</div>
      <div class="panel-body">
      </div>
    </div>
  </div>
 
   
  <% else %>
  <%= render 'devise/sessions/new' %>

  <% end %>
  <% if user_signed_in? %>
 
  <% else %>
  <div class="col-sm-8">
    <div class="holder">
      <h3 class="holder-head"><%= fa_icon 'child' %> New to Streemfeed?</h3>


    </div>
  </div>
  <% end %>
  <% if user_signed_in? %>

  <% end %>


